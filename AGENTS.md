# AGENTS.md — TDT评审专家质量项目约定

本文件只记录本项目专属约定；同时遵循用户级全局 `C:\Users\siyuan.zhang\.agents\AGENTS.md`，并继承 `D:\ai-workspace\AGENTS.md` 与 `D:\ai-workspace\projects\AGENTS.md`。项目级规则可在本项目范围内补充或收紧上级要求，但不得取消全局红线、跨项目端口登记和缺陷修复证据门槛。

## 项目定位

- 本项目设计TDT技术项目TDR1、TDR2、TDR3评审专家的记录、考评与AI汇总方案。
- 考评结果只作为专家池管理、人才盘点及HRBP判断的参考，不是绩效、晋升或薪酬制度。考评周期由HR和部门经理自行决定，不在本项目范围内。
- 不在示例、测试数据或公开文档中使用真实专家姓名。可默认为虚拟姓名

## 本地服务身份与端口

- 本项目固定使用 `project_id=tdt-expert-review-quality`，默认端口段为 `8865—8899`；不得占用工作区内其他项目已登记的端口段。
- `/api/health` 必须同时返回固定项目身份和随前后端运行源码变化的构建标识。启动脚本只有在两者均匹配时才可复用已有服务；其他项目或未知进程占用端口时不得打开、复用或终止。
- `start.cmd` 含 22 个 `goto`、12 个标签和 `for /f` 括号块，cmd.exe 按行尾切行并按文件偏移跳转，检出为 LF 时会读到半截语句报碎片错误；行尾由仓库根 `.gitattributes` 保障，见全局「Git 规则」。
- `start.cmd` 的启动验收条件：与工作区内其他项目的本地服务同时运行时互不干扰，且重复双击只复用本项目。

## 事实来源

- TDRX评审表是评审事实的来源。
- 每场所需评审人以该场TDRX评审人列表为准；TDR1／TDR2／TDR3名单可以不同，均视为项目实际需求，不与外部需求表交叉判定遗漏。
- 项目评审专家需求表只可用于模拟数据准备，不得作为正式考评的数据来源、应参评名单或缺失校验依据。
- TDRX评审表可通过本地 Excel 文件或飞书云文档 URL 输入；两种来源均可用于正式计分，不得在规则、原型或实现中默认只有一种。
- 专家考评记录是AI系统内部数据结构，不再是需要人工维护的表格。
- 评审过程表现由AI全自动判定；专业价值贡献的档位必须由人工作答问卷选定，AI不得预填档位。

## 文档分工（单一事实源）

| 文件 | 唯一负责 | 不应包含 |
|---|---|---|
| `docs/tdr-...-assessment-by-project-v0.2.md` | 历史按项目评分的规则、档位含义、业务口径 | 字段类型、Excel公式写法 |
| `docs/tdrx-...-data-spec-by-project-v0.2.md` | 按项目版的字段、枚举、公式、AI校验 | 规则叙述（引用对应《办法》章节号即可） |
| `docs/tdr-...-assessment-by-year-v0.4.md` | 年度集中评分方案的规则、档位含义、业务口径；客观分数已更新，主观分数确认前保持讨论稿状态 | 字段类型、接口实现细节 |
| `docs/tdrx-...-data-spec-by-year-v0.4.md` | 年度版的事实卡、问卷、汇总字段、公式和AI校验 | 重复叙述评分规则 |
| `docs/tdrx-form-change-record-v1.0.md` | 评审表改造过程、验证结果、待同步项 | 规则与字段定义（引用《办法》《规范》即可） |
| `CONTEXT.md` | 术语一句话定义 | 完整规则、档位表 |
| `ROADMAP.md` | 进度、待办、关键决议 | 规则细节 |

同一计分周期内规则冲突时以对应《办法》为准。现有试用系统的客观分数按年度版V0.4运行，主观问卷暂沿用年度版V0.3；浏览器验收与主观分数确认完成前不得写成正式上线。

## 一致性维护

- 调整评分维度、权重或公式时，先改对应周期的《办法》，再同步对应《规范》、`CONTEXT.md` 和 `ROADMAP.md`。
- 未确认的规则必须标记为「待确认」，不得写成已经生效的要求。
- 完成文档修改后，至少核对：过程表现三项合计50、年度服务贡献两项合计10、客观分数60、主观分数40、年度总分最高100、跨项目分母为项目数；年度等级只在全体专家的维度一和维度二均完成后统一排名，S＝100，前15％为A、中间70％为B、后15％为C。

## 草稿与预览文件

- 凡与本项目相关的界面草稿、对话内预览、可视化或其他非正式产物，也必须保存到本项目路径内，便于直接查看；不得只放在系统临时目录或项目外路径。
- 文档、界面原型和设计预览默认放入 `docs/`；其他产物按项目目录职责放入对应子目录。
- 草稿文件名须明确包含 `draft` 和版本号；草稿落盘不代表方案已正式生效，确认状态仍以 `ROADMAP.md` 和正式规则文档为准。
- 允许在系统临时目录生成验证中间文件，但最终向用户展示或交付的版本必须同步保存到项目路径内。

## 同事验证包

- 默认不再生成或更新同事验证包；当前同事电脑配置无法运行本系统，日常开发、修复和界面调整只交付项目内代码、文档与验证结果。
- 只有用户明确要求输出验证包时，才生成新的压缩包并执行对应的文件清单、CRC、中文文件名UTF-8标志和启动脚本行尾检查。
- `output/` 中既有试用包仅作为历史产物保留，不因后续普通修改自动重建。
