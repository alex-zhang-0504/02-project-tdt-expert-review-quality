# 行尾规范：跟解释器走，不跟操作系统走。
# 显式声明后，任何机器 clone 都得到正确行尾，不依赖各自的 core.autocrlf。

# 默认：入库统一 LF，检出统一 LF
* text=auto eol=lf

# Windows 解释器：cmd.exe 按 CRLF 切行，LF 会导致 goto/标签/括号块偏移错位
*.cmd  text eol=crlf
*.bat  text eol=crlf
*.ps1  text eol=crlf

# POSIX 解释器：CRLF 会让 shebang 失效（bad interpreter: ^M）
*.sh      text eol=lf
*.command text eol=lf

# 二进制：禁止任何行尾转换，否则文件损坏
*.xlsx binary
*.xls  binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.pdf  binary
*.docx binary
*.pptx binary
*.zip  binary
