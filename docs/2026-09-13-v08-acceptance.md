# V0.8计分与表格交互验收

日期：2026-09-13。业务以[考核办法V0.8](tdr-expert-review-assessment-v0.8.md) 为准。

- 过程：出勤5、会签10、意见10，按阶段4∶2∶4加权，再加参与度5；主观六项10／15／15／10／10／10。
- 奖励：超额意见每条1分、含对策每条2分，均不加权；疑似默认纳入试算，排除仅改变对策奖励。总分100封顶，缺失不默认0分。
- 166项Python测试通过，包含94分规则算例、174→100封顶、对策待识别不阻断基础分、新增普通问题不降分、导出字段与分数一致；Node主副表渲染断言通过。
- 浏览器使用独立模拟服务和两份虚拟Excel导入，合计6场、20位虚拟评审人。虚拟青竹实参6场、意见8条、对策2条：30＋60＋2＋4＝96分；人工排除一条对策后30＋60＋2＋2＝94分。完成实际分数导出操作。
- 工具栏在1280像素宽度下，搜索、视图、批次编号、识别按钮与导出按钮同排。390像素自然换行，整页无横向溢出。
- 主表和副表的横向条独立于纵向内容，底部粘附；1280／390像素检查顶部、中部、底部位置和横向同步。主表窄屏中部条底844px、底部约677px；副表窄屏条底始终约801px（视口844px），滚动到纵向底部后仍可横向操作。
- 复现并修正首次进入隐藏面板导致滚动条误隐藏；副表因ID样式覆盖而出现新旧双横向条，已加精确样式限定，最终只显示一条。
- 副表不再含「查看证据」或拆分前整段原文，对策等于意见时使用「见上述意见」；来源、判定理由、人工记录和识别版本直接展示。底层原文与导出证据保留。
- 本轮未调用真实模型、未使用真实报告，未改动正式全量AI链路或启动脚本，不生成同事验证包。
- 实际导出请求经HTTP重新取回并读取Excel，确认过程30、专业60、超额意见2、对策奖励2、总分94；事实导出的含对策条数为整数格式，28处当前文档链接检查通过。
- 最新普通服务在8875端口启动，项目身份及构建标识28688978d07ef1de匹配；真实浏览器打开首页成功。旧8872服务保留。

预览：[工具栏与主表](2026-09-13-toolbar-scroll-draft-v0.8.png) 、[窄屏副表横向条](2026-09-13-detail-scroll-draft-v0.8.png) 。
